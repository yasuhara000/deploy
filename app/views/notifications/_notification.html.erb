
<% if user_signed_in? %>
  <% visiter = notification.e_visiter %>
  <% question = notification.question %>

  <div class="user-view clearfix ">
    <%= link_to engineer_path(notification.e_visiter) do %>
      <%= attachment_image_tag visiter, :profile_image, :fill,100,100, format: "jpeg", fallback: "no_image.jpg", size: "50x50", class:"profile-img-circle" %>
    <% end %>
    <%= notification_form(notification) %><span class="moderate-font"><%= " (#{time_ago_in_words(notification.created_at)} 前)" %></span>
    <br>
    <% if !@answer.nil? %>
      <p class="moderate-font text-center" style="color: #C0C0C0;"><%= @answer %></p>
    <% end %>
  </div>
<%elsif engineer_signed_in? %>
  <% visiter = notification.visiter %>
  <%question = notification.question %>

  <div class="user-view clearfix ">
    <%= link_to user_path(notification.visiter) do %>
      <%= attachment_image_tag visiter, :profile_image, :fill,100,100, format: "jpeg", size: "50x50", class:"profile-img-circle" %>
    <% end %>
    <%= notification_form(notification) %><span class="moderate-font"><%= " (#{time_ago_in_words(notification.created_at)} 前)" %></span>
    <br>
    <% if !@answer.nil? %>
      <p class="moderate-font text-center" style="color: #C0C0C0;"><%= @answer %></p>
    <% end %>
  </div>
<%end%>